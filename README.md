Put the file `antiidle.vbs` in `Start button`->`All Programs`->`Startup` and restart or manually trigger.  
The script will simulate clicking `scroll lock` and click again after 1 second. The cycle is 10 min 1 sec.
